{
  "name": "react-node-inject",
  "version": "1.0.2",
  "description": "Inject toasts, modals, and more on the fly - all in React!",
  "module": "true",
  "repository": "https://github.com/ryanhaticus/react-node-inject",
  "scripts": {
    "build": "npx tsc",
    "publish": "pnpm build && cp ./package.json ./out && cd ./out && npm publish"
  },
  "keywords": [
    "react",
    "inject"
  ],
  "typings": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "author": "Ryan 'Haticus' Huellen",
  "license": "MIT",
  "dependencies": {
    "react": "^18.2.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.15",
    "tslib": "^2.4.0"
  }
}
